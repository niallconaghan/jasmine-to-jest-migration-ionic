import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'jest-migration-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
